function scrollToBottom ()
{
    var height = document.body.scrollHeight; /*makes a variable called height and equats it to the scroll height of the page*/
    window.scroll(0, height); /*Makes the window scroll down to variable height, which equals the final height of the page */
}